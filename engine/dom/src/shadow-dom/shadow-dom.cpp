#include "wind/dom/shadow-dom/shadow-dom.hpp"

namespace wind::dom::shadow {
  // Root init(glm::ivec2 windowSize) {
  // ElementVariants variant = ElementVariants(Root());
  // Root a = Root();
  // std::make_shared<Element>(shadow::Element(ElementPtr(std::make_shared<Root>()))
  // );
  // auto root = wind::share(a);
  //   return a;
  // };

} // namespace wind::dom::shadow
