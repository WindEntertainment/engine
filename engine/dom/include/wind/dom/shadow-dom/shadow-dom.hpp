#pragma once
#include "shared.hpp"
#include "wind/dom/shadow-dom/components/index.hpp"

namespace wind::dom::shadow {
  // Root init(glm::ivec2);

  // template <typename T>
  // concept Element = std::is_base_of<UIElement, T>::value;

  // template <Element T>
  // template <typename T>
  // T createElement() {
  // ElementVariants variant = ElementVariants(T());
  // T a = T();

  // std::shared_ptr<Element> a =
  //   std::make_shared<Element>(Element(ElementPtr(std::make_shared<T>())));

  // std::shared_ptr<EFIX> a =
  // std::make_shared<EFIX>(shadow::EFIX(ElementPtr(std::make_shared<Root>()))
  // );
  // auto element = std::make_shared<EFIX>(T());
  // ElementPtr a = ElementPtr(std::make_shared<T>());
  // auto element = std::make_shared<EFIX>(a);

  // return a;
  // };
} // namespace wind::dom::shadow
